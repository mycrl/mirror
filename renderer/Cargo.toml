[package]
name = "renderer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "renderer"

[dependencies]
anyhow = "1.0.82"
bytes = "1.5"
log = "0.4.20"
common = { path = "../common" }
mirror = { path = "../mirror" }
