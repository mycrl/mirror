[package]
name = "renderer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "renderer"

[dependencies]
anyhow = "1.0.86"
common = { path = "../../common" }
rodio = "0.19.0"
sdl2 = { version = "0.37.0", features = ["bundled", "static-link", "raw-window-handle"] }
log = "0.4.20"
